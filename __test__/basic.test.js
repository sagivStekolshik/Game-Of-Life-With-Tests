const createMatrix = () => ({})
describe('checking the matrix', () => {
    
    
    beforeEach(() => {
        this.matrix = createMatrix()
    })

    test('new matrix is empty', () => {
        expect(this.matrix).toStrictEqual({})
    })

    test('can place random points', () => {
        
    })


})